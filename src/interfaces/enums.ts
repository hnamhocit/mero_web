export enum Role {
  USER = "USER",
  ADMIN = "ADMIN",
}

export enum Sex {
  MALE = "MALE",
  FEMALE = "FEMALE",
  UNKNOWN = "UNKNOWN",
  OTHER = "OTHER",
}

export enum ConversationType {
  DIRECT = "DIRECT",
  GROUP = "GROUP",
}

export enum ParticipantRole {
  ADMIN = "ADMIN",
  SUB_ADMIN = "SUB_ADMIN",
  MEMBER = "MEMBER",
}
